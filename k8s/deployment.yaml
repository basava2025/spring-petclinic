apiVersion: apps/v1
kind: Deployment
metadata:
  name: petclinic
spec:
  replicas: 2
  selector:
    matchLabels:
      app: petclinic
  template:
    metadata:
      labels:
        app: petclinic
    spec:
      containers:
      - name: petclinic
        image: basavab413/petclinic-app:v1
        ports:
        - containerPort: 8080

        resources:
          requests:
            memory: "256Mi"
          limits:
            memory: "512Mi"

        env:
        - name: JAVA_TOOL_OPTIONS
          value: "-Xmx256m -Xms128m"

        # ✅ PostgreSQL configuration (FIXED)
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://demo-db:5432/petclinic

        - name: SPRING_DATASOURCE_USERNAME
          value: user

        - name: SPRING_DATASOURCE_PASSWORD
          value: pass

        - name: SPRING_DATASOURCE_DRIVER_CLASS_NAME
          value: org.postgresql.Driver
